"""应用版本号 —— **唯一权威来源（Single Source of Truth）**。

## 命名规则
`v` + `YYYYMMDD`（发布日）+ 3 位「提交总数」（`git rev-list --count HEAD`，提交后自洽）
例：`v20260911150`

## 为什么版本号放在后端、并由后端下发 ★

历史上前端把版本号硬编码在 HTML 里，登录后页头显示的就是那个字符串。这有个致命盲区：
**它只能证明「前端文件是哪一版」，完全无法反映「正在运行的云端后端是哪一版」。**

而本项目**后端也是双副本**：真正的部署包是 `publish/`，`publish/start.sh` 用
`uvicorn --app-dir backend app.main:app` 启动 → 线上跑的是 **`publish/backend/`**，
不是开发用的 `backend/`。一旦某次发版只同步了前端、忘了同步 `publish/backend/`，
页面照样显示最新版本号，用户会以为一切都已更新，实际后端还是旧代码（新接口 404、
新字段缺失，且大多被前端「失败静默」吞掉，很难察觉）。

因此改为：
  后端在 `GET /api/health` 里**自报版本** → 前端展示**后端返回的**版本 →
  前后端版本不一致时前端**显式告警**。
这样「看到的版本 = 真正在跑的后端版本」，双副本不同步会立刻暴露，而不是静默出错。

## ⚠️ 关于代码注释里的 `vNNN`

`v189 / v190 / v191 / v192 …` **不是版本号**，是历史遗留的**需求批次标签**，与版本号
是两套计数（批次 192 vs 版本 149），极易混淆（用户已因此误判过一次）。
**今后新增注释请写「日期+批次」，例如「0911批次4」，禁止再写 `vNNN`。**
既有 `vNNN` 注释保留不动（它们标记功能引入时点，是有效的历史信息）。

## 发版前必做
`python scripts/version_bump.py --check` —— 校验 version.py / 前端常量 / 两份 publish
副本是否一致；不一致会非零退出并给出修复命令。
"""
from __future__ import annotations

# 修改版本请用 scripts/version_bump.py --bump（会同步改前端常量），不要只手改这里。
APP_VERSION = "v20260912151"
